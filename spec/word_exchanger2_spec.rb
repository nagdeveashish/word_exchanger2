require 'spec_helper'

describe WordExchanger2 do
  it 'has a version number' do
    expect(WordExchanger2::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
